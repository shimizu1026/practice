# 練習問題


## 1. 「index.html」に「execises」› 「assets」 › 「styles」フォルダにある「global.css」をリンク


## 2. 以下を参考に関数を使って、ウインドウサイズによるサイズの可変を行う

   | セレクタ | プロパティ | 比較関数 |  
   | --- | --- | --- |
   | .header | `padding-block` | ビューポートが375pxの時 `--space-sm`、 ビューポートが1440pxの時 `--space-lg`　|
   | .main| `margin-block-start` | ビューポートが375pxの時 `--space-lg`、 ビューポートが1440pxの時 `--space-xl`　|
   | .footer | `padding-block` | ビューポートが375pxの時 `--space-lg`、 ビューポートが1440pxの時 `--space-xl`　|
  | .site-name | `font-size` | ビューポートが375pxの時 `--font-md`、 ビューポートが1440pxの時 `--font-lg`　|
  | .button-hamburgerの before擬似要素とafter擬似要素 | `inline-size` | ボタンサイズの半分　|
  | .page-title | `font-size` | ビューポートが375pxの時 `--font-xl`、 ビューポートが1440pxの時 `--font-2xl`　|
   | .services| `margin-block-start` | ビューポートが375pxの時 `--space-lg`、 ビューポートが768pxの時 `--space-xl`　|
   | ^^ | `row-gap` | ビューポートが375pxの時 `--space-md`、 ビューポートが768pxの時 `--space-lg`　|
  | .service-name | `font-size` | ビューポートが375pxの時 `--font-lg`、 ビューポートが1440pxの時 `--font-xl`　|
