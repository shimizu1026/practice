# 練習問題


## 1. 「index.html」に「execises」› 「assets」 › 「styles」フォルダにある「global.css」をリンク


## 2. 完成イメージをよく観察し、以下のヒントを参考にしながら3Dのサイコロを作成（色、大きさなどが違っても問題ない）

   -  `grid` または`position`を使って 全ての面をサイコロの要素内の同じ場所に同じ大きさで重ねる
   - 3D空間に配置するモードに設定
   - 面を1つずつトランスフォーム系のプロパティ（平行移動や回転）を活用して配置
   - サイコロの要素自体も回転軸を調整して回転させる
   