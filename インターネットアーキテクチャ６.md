アプリケーション層

WEBブラウザ、電子メールなどのアプリケーション特有の通信処理を行う

HTTP、FTP、SMTP、POP等のプロトコルがかかわる

FTP
ファイルトランスファープロトコル
異なるコンピュータ間でファイル転送を行うプロトコル
ログインする際のユーザ名とパスワードは暗号化されないのであんまり使わないほうがいい
ポート番号２０（転送）、２１（接続管理）の２つのポート番号を使う

代替案
FTPS
FTP over SSL/TLS

SFTP
SSHを使ったFTP（鍵がある）
よりセキュア
なるべくこっち

FTPクライアントソフト
サイバーダックやファイルジリア

VSコードのプラグインでSFTPがある
ジェイソンファイルでパスワードなどを保存しているとVSコードからアップロードできる
基本上書き保存なので注意

ターミナルでも可能

公開フォルダの中にアップしないといけない
Xサーバーはパブリックフォルダ
さくらはwww

電子メール
メールアドレスを使ってネットワーク上で郵便を送る
昔
SMTPを使って直接送信　相手のPCの電源はいってなかったら送れない

メールサーバを経由する電子メールになった
WebサーバはあくまでHTML等のファイルをあげるところ
メールサーバは別

SMTP→メールサーバ→SMTP→メールサーバ
サーバはあくまで窓口のようなもの
サービスはサーバごとに違う
メールサーバは２４時間稼働なのでそこにはいる
POPを使ってメールサーバから手元に持ってくる
送る（SMTP）と受信（POP）を設定しないとメールできない

SMTP
シンプルメールトランスファープロトコル
２５番ポート
電子メールを送信するアプリケーションプロトコル
認証機能なし。
危ないので使うのやめて５８７番ポートを使う
認証機能を付けたSMTP
認証にはSMTP-AUTHを利用。

POP
ポストオフィスプロトコル
メールサーバから受信するプロトコル
110番ポートを使う
暗号化通信はされていない
995番ポート　POP3S　セキュリティが高い方
受けとったらメールサーバには残らない
1か所でしか見れない

IMAP
インターネットメッセージアクセスプロトコル
メールをどこでも見れるようにしたい
143番ポート
メールサーバにメールは残っている状態で自分の受信箱にメールを同期する？感じ
メールサーバで管理される
削除したらメールサーバから消える
メールサーバを自分で整理しないとたまるのがデメリット
993番ポートがセキュリティが高い