トランスポート層
アプリケーションプログラム間の通信を実現する
例：相手が受け取ったことを確認できる形態で送るのか、一方的に送るのか、ブラウザで複数タブを開く

ポート番号
船場。コンピュータまではIPアドレスで送るのは分かる。どのソフトに送るのか、アプリケーションを識別するための番号。
サーバ：サービスを提供。80番ポートを使うと決められている。
HTTPSは443番ポート。
役所や銀行の窓口みたいなもの。
サーバによってやってるサービスは違う。
well-knownポート：標準でポート番号が決まっている。HTTPは80番、HTTPSは443。
Registeredポート：登録済みのポート。自分がつくったものを見えるようにするのに使うポート。

クライアント側
Dynamicポート

ローカルホスト　5500番とか

well-knownポート
20番　FTP（データ転送）
21番　FTP（データコントロール）
22番　SSH
25番　SMTP　メール送信　認証はない
80番　HTTP
110番　POP3　メール受信
143　ＩＭＡＰ
443　ＨＴＴＰＳ

コネクション型
相手と通信を確認しながらやりとりする通信
官製はがき、電話
データの欠落が起きにくい

コネクションレス型
送信して届いたかは不明
ポスティングされるチラシなど

UDP
コネクションレス型の通信を行うプロトコル
一方的にデータを送る。確認はしない。
届けるスピード重視
音声や映像などリアルタイム性のあるデータ、複数の相手に同じデータを転送、マルチキャスト通信、ブロードキャスト通信、信頼性が必要なく少量のデータを転送する（DNSサーバとの通信）

TCP
コネクション型の通信を行うプロトコル。
今から送る→OK→コネクション設立→送る→届く→ACK（アック）届いた証明が来る→切断します→OK→切断
という流れ


